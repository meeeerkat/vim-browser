#include "model/nodes/ul.hpp"

namespace Nodes {
    Ul::Ul(const GumboElement *el, BuildData &build_data)
        : Element(el, build_data)
    {

    }

    void Ul::printw(WINDOW *window, PrintingOptions &printing_options)
    {

    }
}
